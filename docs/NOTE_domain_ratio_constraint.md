# QuaDMix 域配比结构性约束分析

> 2026-06-29 | 发现：QuaDMix 搜索无法有效改变域比例，搜索空间在域配比维度几乎退化

## 1. 问题

QuaDMix-BMK 中训效果不够好。怀疑：搜索出来的"最优配比"是否真正改变了域分布？如果原始数据集由少数大域主导数据量，采样机制是否天然限制了域比例的可调范围？

## 2. 机制分析

### 2.1 采样公式

```
S(r̄) = (2/(1+e^{-λ(ω-r̄)}))^η + ε,  if r̄ <= ω
S(r̄) = ε,                             if r̄ > ω
```

- S_max ≈ 2^η + ε ≤ **2.001**（最多重复 2 次）
- S_min = ε ≈ 0.001（几乎不选）
- 对每个域独立计算，参数 (λ, ω, η, ε) 按域配置

### 2.2 搜索空间

搜索空间包含：
1. **质量权重** `domain_weights`：控制每个域内如何评分质量
2. **采样参数** `(λ, ω, η, ε)`：控制每个域内的采样曲线形状

**关键发现：没有任何参数直接控制"最终数据集中域 A 占 X%，域 B 占 Y%"。**

最终域分布是以下公式的副产品：

```
域 m 最终 token 数 = Σ_{doc ∈ m} S(r̄_doc) × tokens_doc
```

### 2.3 理论约束

| 域 | 原始文档数 | 占比 | 最大放大 (S=2) | 最小缩小 (S=ε) | 理论最大占比 |
|----|-----------|------|--------------|--------------|------------|
| D0 (Industrial) | 6.66M | 41.1% | 13.3M | 6.7K | ~48% |
| D1 (Social) | 3.56M | 22.0% | 7.1M | 3.6K | ~26% |
| D9 (Arts) | 2.98M | 18.4% | 6.0M | 3.0K | ~22% |
| D7 (General) | 1.41M | 8.7% | 2.8M | 1.4K | ~10% |
| D6 (History) | 455K | 2.8% | 910K | 455 | ~3% |
| D2 (Science) | 375K | 2.3% | 750K | 375 | ~3% |
| D5 (Literature) | 204K | 1.3% | 409K | 204 | ~1.5% |
| D3 (Religion) | 297K | 1.8% | 594K | 297 | ~2% |
| D8 (Philosophy) | 167K | 1.0% | 334K | 167 | ~1.2% |
| D4 (Language) | 83K | 0.5% | 167K | 83 | ~0.6% |

理论上搜索可以让大域缩到几乎 0，小域放大 2 倍。但实际上采样曲线是 sigmoid 形状，大部分文档的 S 值在 0.1-1.5 之间，搜索能调的只是曲线形状，不能对每个文档独立控制。

## 3. 实测数据

### 3.1 Pipeline 输出（`pipeline_summary.json` → `sampling.domain_distribution_change`）

| 域 | 原始文档数 | 选中数 | 采样率 | 原始占比 | 选中占比 | 占比变化 |
|----|-----------|--------|--------|---------|---------|---------|
| D0 (Industrial) | 6,664,930 | 161,227 | 2.42% | 41.1% | 36.1% | -5.0pp |
| D1 (Social) | 3,561,078 | 99,629 | 2.80% | 22.0% | 22.3% | +0.3pp |
| D9 (Arts) | 2,982,146 | 135,599 | 4.55% | 18.4% | 13.5% | -4.9pp |
| D7 (General) | 1,408,872 | 91,313 | 6.48% | 8.7% | 16.1% | **+7.4pp** |
| D6 (History) | 455,332 | 22,910 | 5.03% | 2.8% | 5.1% | +2.3pp |
| D2 (Science) | 375,207 | 22,506 | 6.00% | 2.3% | 5.0% | +2.7pp |
| D3 (Religion) | 296,968 | 12,478 | 4.20% | 1.8% | 2.8% | +1.0pp |
| D5 (Literature) | 204,481 | 7,555 | 3.69% | 1.3% | 1.7% | +0.4pp |
| D8 (Philosophy) | 167,144 | 5,683 | 3.40% | 1.0% | 1.3% | +0.3pp |
| D4 (Language) | 83,450 | 4,941 | 5.92% | 0.5% | 1.1% | +0.6pp |

### 3.2 关键数据

- **采样率范围**：2.42% ~ 6.48%，最大/最小比仅 **2.7x**
- **整体采样率**：~3.5%（564K / 16.2M）
- **大域仍主导**：D0 + D9 合计仍占选中数据的 **49.6%**
- **小域无法放大**：D4 从 0.5%→1.1%，D8 从 1.0%→1.3%，变化微乎其微

## 4. 结论

### 4.1 核心问题

**QuaDMix 的搜索空间在域配比维度几乎退化。**

874 个 proxy 实验产生的域配比变化极小（采样率 2.4%-6.5%），LightGBM 在这个维度上学不到有意义的信号。搜索本质上做的是"按质量选择性下采样"，而不是"重新配比域混合比例"。

### 4.2 根因

1. **无显式域权重参数**：搜索空间只有质量权重和采样曲线参数，没有直接控制域比例的参数
2. **采样曲线同质化**：所有域共享同一质量评分体系，质量分布相似 → 采样曲线相似 → 各域平均 S 值相近
3. **S 值范围受限**：S ∈ [ε, 2.001]，即使极端参数也只能让单域变化 2/ε 倍，但 sigmoid 形状使实际有效范围远小于理论范围

### 4.3 影响

- 搜索的"最优"是在原始分布约束下的**局部最优**
- 如果最优配比需要大幅改变域比例（如 Math 从 0.27% 提升到 10%），QuaDMix 根本到不了那个点
- LightGBM 预测的 loss 曲面在域比例维度上几乎是平的

## 5. 修复方向

### 5.1 引入显式域权重

在采样公式中加入域级权重参数：

```
最终采样值 = domain_weight[m] × S(r̄)
```

其中 `domain_weight[m]` 是搜索空间中的显式参数（如 Dirichlet 分布采样），直接控制域间比例。

### 5.2 两阶段搜索

1. **阶段 1**：搜索最优域配比（低维，如 10D simplex）
2. **阶段 2**：在固定域配比下，搜索最优质量采样参数

### 5.3 预处理阶段均衡化

在 QuaDMix 之前，先按目标比例对各域进行预采样（如均匀分布或 Chinchilla 比例），再用 QuaDMix 做域内质量筛选。

### 5.4 与 L2 映射的关系

L2 映射（10→24 类）不会解决这个结构性问题。维度翻倍反而使搜索空间更稀疏，每个域的数据量更小，域比例约束更紧。**应先解决域配比约束，再考虑 L2 细分。**
